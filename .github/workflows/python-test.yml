name: build and test

on:
    push:
        branches:
            - "main"
    pull_request:
        branches:
            - "main"
            
jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: start
              run: echo "start test"
            
